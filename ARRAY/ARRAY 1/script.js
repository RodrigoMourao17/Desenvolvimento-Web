const frutas = ['maçã', 'banana', 'laranja', 'uva', 'melancia'];
const container = document.getElementById("frutas_ordem1");

container.textContent = "Texto JS =  " + frutas[0] + " e " + frutas[frutas.length -1];
